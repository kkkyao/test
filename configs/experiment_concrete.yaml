experiment:
  name: beers_concrete
  max_steps: 50
  history_window: null
  auto_evaluate: true

logging:
  output_dir: outputs/beers_concrete
  save_steps: true
  save_trajectory: true
  save_interaction_log: true