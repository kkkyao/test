experiment:
  name: beers_text_concrete
  max_steps: 10
  history_window: null
  auto_evaluate: false

logging:
  output_dir: outputs/beers_text_concrete
  save_steps: true
  save_trajectory: true
  save_interaction_log: true