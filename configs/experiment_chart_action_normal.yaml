experiment:
  name: chart_action_normal
  task_mode: student_simulation
  max_steps: 50
  history_window: 10
  auto_evaluate: false
logging:
  output_dir: outputs/chart_action_normal
  save_steps: true
  save_trajectory: true
  save_interaction_log: true
visual:
  observation_mode: text_image
  template_path: templates/chart_state_view.html
  output_subdir: images
  normalize_bars: true
  playwright:
    headless: true
    slow_mo: 0
  image_history_window: null