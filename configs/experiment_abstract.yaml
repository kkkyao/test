experiment:
  name: beers_text_abstract
  max_steps: 10
  history_window: null
  auto_evaluate: false

logging:
  output_dir: outputs/beers_text_abstract
  save_steps: true
  save_trajectory: true
  save_interaction_log: true