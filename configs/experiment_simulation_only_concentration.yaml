experiment:
  name: simulation_student_concentration
  task_mode: student_simulation
  max_steps: 50
  auto_evaluate: false
  history_window: 10

visual:
  observation_mode: simulation_only
  simulation_type: concentration
  image_history_window: 5
  output_subdir: simulation_images
  template_dir: templates/simulations

  viewport:
    width: 900
    height: 500

  playwright:
    headless: true
    slow_mo: 0

logging:
  output_dir: outputs/simulation_student_concentration
  save_steps: true
  save_trajectory: true
  save_interaction_log: true