experiment:
  name: text_only_student
  task_mode: student_simulation
  max_steps: 50
  history_window: 10
  auto_evaluate: false

logging:
  output_dir: outputs/text_only_student
  save_steps: true
  save_trajectory: true
  save_interaction_log: true